# コーディング規約

このドキュメントは、NicheLibrary のヘッダファイル、verify ソースコード、ドキュメント、スクリプト、設定ファイルなどに適用されるコーディング規約である。

このドキュメントでは、[docs/](docs/) または [.competitive-verifier/docs/](.competitive-verifier/docs/) 配下のドキュメントを「ライブラリのドキュメント」と呼び、その他のドキュメント（特に [README.md](README.md)、[CONTRIBUTING.md](CONTRIBUTING.md)、[AGENTS.md](AGENTS.md)、[STYLE_GUIDE.md](STYLE_GUIDE.md)）を「リポジトリのドキュメント」と呼ぶ。

## 共通

- 全てのテキストファイルの末尾には改行を入れる。
- 句読点は基本的に `、` と `。` とする（`,` と `.` などでない）。
  - ただし、`Library Checker (Yosupo Judge), yukicoder, AOJ` のように英数字どうしを並べる際のカンマまで避ける必要はない（どちらでもよい）。
- 複数の語句を並べる際は、区切り記号に `、` または `, ` を使用し、上の規約にも従う（`・` や `  /  `、`/` などでない）。
  - ただし、TeX 数式中の `,` の後ろには空白はあってもなくてもよい。
- ソースコード中のコメントやドキュメントなどの文章は日本語で記述し、次に示す例外を除いて文体を常体で統一する（`です`、`ます` などの敬体を使用しない）。
  - [pyproject.toml](pyproject.toml) の `[project]` 内の `description` および [.editorconfig](.editorconfig), [.gitattributes](.gitattributes), [.gitignore](.gitignore) 内のコメントは英語で記述する。
  - 参考文献を記述する場合は、人名も含めて英語で記述する。
  - 同じ内容を日本語でも記述している場合は、英語などの他の言語を併記してもよい。
- リポジトリの [README.md](README.md) や [CONTRIBUTING.md](CONTRIBUTING.md)、GitHub Pages 上のドキュメントのトップページ（[.competitive-verifier/docs/index.md](.competitive-verifier/docs/index.md) を基に生成される）の文章は日本語で記述し、次に示す例外を除いて文体を敬体で統一する（`だ`、`である` などの常体を使用しない）。
  - ただし、箇条書き内の文章は常体でも敬体でもよいとする（少なくとも深さが最も小さい部分の文章は常体を推奨するが、厳密に片方に決める運用はしないものとする）。
- プログラム、スクリプト、コンパイラが出力するエラーメッセージは英語で記述する。
- 「ならば」と書くべき箇所で「なら」と省略しない。
- ハイフン、エンダッシュ、エムダッシュを使い分ける。
  - 人名どうしを結ぶ記号や、期間の開始と終了を結ぶ記号などには、ハイフンやエムダッシュではなくエンダッシュを使用する。
  - 文章中に補足を挿入する場合などには、ハイフンやエンダッシュではなくエムダッシュを使用する。
  - ただし、固有名詞の正式な表記がこの使い分けに従っていない場合は、その表記を優先する。
  - また、[LICENSE](LICENSE) 中の記述も例外とする。
- 文章中の引用符は `‘ ’` と `“ ”` とする（`' '` と `" "` などでない）。
  - ただし、C++ の文字リテラルや文字列リテラルを表す場合の引用符は `' '` と `" "` でよい。
  - また、固有名詞の正式な表記がこの使い分けに従っていない場合は、その表記を優先する。
  - また、[LICENSE](LICENSE) 中の記述も例外とする。
- 文章中のアポストロフィーは `’` とする（`'` などでない）。
  - ただし、`Luzhiled's Library` や `Nyaan's Library` のような固有名詞は例外とする。
  - また、[LICENSE](LICENSE) 中の記述も例外とする。
- indexing は `0-based indexing`, `1-based indexing` のように `k-based indexing` の形で記述する（`k-indexed` などでない）。
- かぎかっこは「引用を表す囲み」または「囲まないと入れ子になって読みにくい部分を表す囲み」としてのみ使い、強調などに濫用しない。
- 数式中で使用する文字（一般的な数学関数や数学定数を除く）は、その数式よりも前に説明する。
  - ただし、Jekyll Front Matter の `title` 中の数式は例外とする。

## パス、ファイル名

- C++ ソースコードのディレクトリ名およびファイル名では、単語をハイフンで区切る（アンダースコアなどでない）。
- Python ソースコードのディレクトリ名およびファイル名では、単語をアンダースコアで区切る（ハイフンなどでない）。
- ルート直下に全てのヘッダファイルを 1 つにまとめるディレクトリを置かない。
- ドキュメントは、対応するヘッダファイルと同じディレクトリ構成になるよう、[docs/](docs/) 配下に置く。
- verify ソースコードは [verify/](verify/) 内に置く。
  - それぞれの verify ソースコードのファイル名には、`yosupo`, `yukicoder`, `aoj`, `standalone` のいずれかを接頭辞として付ける。
    - 接頭辞が `standalone` 以外であれば、それに続く部分は問題の ID などとする。
    - 接頭辞が `standalone` であれば、それに続く部分は verify を行うヘッダファイルの名前とする（フルパスではなく、basename から拡張子を除いたものでよい）。
  - verify ソースコードの拡張子は `.test.cpp` とする。

## C++ ソースコード（共通）

- 型テンプレート引数にプリミティブな整数型以外も指定できる場合、その型の値を `+= 1` でインクリメントし、`-= 1` でデクリメントする（インクリメント演算子 `++`、デクリメント演算子 `--` を用いない）。ただし、主にイテレータを指定する型テンプレート引数は除く。
- 型テンプレート引数に主にイテレータを指定する場合は、前置または後置インクリメント演算子を使用し、イテレータ以外の型を指定できないようにメタ関数や `static_assert` などを使用する。
- 型テンプレート引数にプリミティブな整数型以外も指定できる場合は、その型の値に対して、ビット演算で行う必要がない演算をビット演算で行わない。
  - たとえば、偶奇判定には `& 1` ではなく `% 2` を用い、2 倍の計算には `<< 1`, `<<= 1` ではなく `* 2`, `*= 2` を用い、2 で割った商の計算には `>> 1`, `>>= 1` ではなく `/ 2`, `/= 2` を用いる。
- clang-format 20 を用いて、[.clang-format](.clang-format) で指定したオプションによりフォーマットする。
- 1 行目を空行にしない。
- ブロック末尾の `}` だけからなる行の直前を空行にしない。
- URL を記述する場合、URL が行末になるように記述する。
  - URL が行末にない場合、competitive-verifier が URL に続く文字列まで URL の一部とみなしてドキュメントを生成してしまうため。
- 型テンプレート仮引数を宣言するときは `class` キーワードを使用する（`template <typename T>` のように `typename` キーワードを使用しない）。
- このライブラリの他のファイルを include する文よりも先に、標準ライブラリのヘッダを include する。
- `static_assert` のエラーメッセージを省略しない。

## ヘッダファイル

- ヘッダファイルの include guard はファイルの先頭に置き、ドキュメンテーションコメントなどより前に書く。
  - include guard の名前はそのファイルの相対パスを英大文字またはアンダースコアのみで表したものにする。
  - `#pragma once` を使用しない。
- そのファイルで使う標準ヘッダを明示的に列挙する。
  - `bits/stdc++.h` を使用しない。
  - 未使用のヘッダを include しない。
- GCC 拡張や `__gnu_pbds`, `#pragma GCC optimize`, `typeof`, VLA などを使用しない。
- g++ および clang++ の C++20（`-std=c++20`）でコンパイルできるようにする。
  - コンパイラ依存の実装にしない。
- 前提を満たさない入力を受け入れるための処理を過度に入れない。
  - 必要な前提は `assert` などで確認する。ただし、`private` な関数について、全ての呼び出し箇所で必要な前提を満たすことが保証されている場合は、`assert` などによる確認を省略してもよい。
  - 前提を満たす入力に対する性能を著しく落とすような処理を入れない。
- 公開 API には、VS Code などのコード補完で見つけやすい名前を付ける。
  - 用途から思い出しにくい名前や、短すぎる名前にしない。
- 構造体またはクラスの場合、基本的にメンバを `public` にする。
  - メンバ関数から再帰的に呼び出すために引数を追加した補助関数などは、`private` でもよい。
- ヘッダファイルの先頭のコメントには、ヘッダファイルの機能、主な事前条件、計算量などを簡潔に書く。
  - 冗長な一般論や導入文にしない。
- include guard、先頭のコメント、標準ライブラリについての include 文、このライブラリの他のファイルについての include 文、実装の先頭の各区切りには、空行を 1 行ずつ入れる。
- 実装の末尾と `#endif` の間には、空行を 1 行入れる。

## verify ソースコード

- verify ソースコードの先頭には `// competitive-verifier: ...` を記述する。
  - Library Checker (Yosupo Judge), yukicoder, AOJ のいずれかの問題を使用するか、`STANDALONE` による自己検証を行う。
    - いずれかのジャッジの問題を使用する場合は `// competitive-verifier: PROBLEM ...` とし、`...` の部分に問題の URL を記述する。
    - `STANDALONE` の場合は `// competitive-verifier: STANDALONE` とする。
  - verify に AtCoder の問題を使用しない。
- verify に小数の誤差を許容する問題を使用する場合は、verify ソースコードの 2 行目に `// competitive-verifier: ERROR ...` を記述する（`...` には許容誤差を小数で指定する）。
- オンラインジャッジ上の問題を使う verify ソースコードと、STANDALONE で行う自己検証を 1 つのファイルにまとめない。
  - オンラインジャッジ上の問題を使う verify ソースコードは、その問題を解くために必要な処理だけで構成する。
  - ライブラリ API の網羅テスト、愚直解との比較、小さい入力の全探索テストなどは `// competitive-verifier: STANDALONE` の verify ソースコードに分離する。
  - ライブラリの用途や想定する計算量に合わない問題を、オンラインジャッジ上の verify 問題として無理に選ばない。
- AOJ の URL は `https://judge.u-aizu.ac.jp/onlinejudge/description.jsp?id=...` の形式とする（`http://...` などでない）。
- 先頭の competitive-verifier 向けのコメント、標準ライブラリについての include 文、このライブラリの他のファイルについての include 文、実装の先頭の各区切りには、空行を 1 行ずつ入れる。
- 標準出力では `std::endl` を使用せず、`'\n'` を出力して改行する。
- `main` 関数の引数は明示的に `void` と記述せず、空にする。
- 標準入出力を行う場合、`main` 関数の最初の 2 行には順に `std::ios::sync_with_stdio(false);` と `std::cin.tie(nullptr);` を記述し、その直後に空行を 1 行入れる。
- `main` 関数内の全ての `return 0;` の前の行には、空行を 1 行入れる。
- `main` 関数の最後の行にも `return 0;` を記述する。

## Python ソースコード

- Ruff でフォーマットし、Mypy, Pyright, Ruff による静的検査でエラーが検出されないようにする。
  - フォーマットや静的検査に用いる具体的なコマンドは、[README.md](README.md) の「Python の検査」の節に記述してある。

## ドキュメント（共通）

- マークダウンとして記述する。
- Prettier を用いてフォーマットする。
- 番号なし箇条書きは `- ` とする（`* ` や `   -  ` などでない）。
- 番号付き箇条書きは常に `1. ` とする（`2. ` 以降は使用しない）。
- TeX 数式の囲みは `$ $` とする（`\( \)` などでない）。
- TeX 数式とその前後の文字との間には、必ず空白または改行を入れる（前後の文字が句読点や括弧であっても、`$` との間に空白を入れる）。
- TeX 数式での絶対値記号による囲みは `\lvert \rvert` とする（`| |` などでない）。
- TeX 数式の等号付き不等号や否定記号は `e` で終わるコマンド（`\ge`, `\le`, `\ne`）で記述する（`\geq`, `\leq`, `\neq` などでない）。
- 複数のインライン数式を続けて並べる場合は、全体を 1 つの TeX 数式として記述する（複数の TeX 数式を数式の外の `,` や `、` などで区切らない）。

## ライブラリのドキュメント

- GitHub Pages 上のドキュメントのトップページは、この節の規約の対象外とする。
- ライブラリのドキュメントは箇条書きを中心とした簡潔な記述にする。
- 先頭に、`title`, `documentation_of` の順に Jekyll Front Matter を記述する。
  - `documentation_of` は対応するヘッダファイルを指すようにする。
  - `title` は人間にとって分かりやすいタイトルにする（TeX 数式を使用してもよい）。
  - `title` は動詞で終わる文にせず、ドキュメントの内容を表す名詞句にする。
- 「前提: 」や「備考: 」などのラベルでは、半角コロンと半角スペースを使用する（「前提：」「前提:」「前提 : 」などでない）。
- 対応するヘッダファイルの include 指令は自動で挿入されるため、ドキュメントには重複して記述しない。
  - 使用例のソースコードなどの、include 指令が実際に必要である例は除く。
- `## 概要`、`## 使い方`、`## 計算量` の節をこの順に記述する。
- `## 使い方` の節では、各公開 API の使い方を説明する。
  - 実装の内部状態を保持するメンバ変数は、`public` であっても記載しなくてよい。
  - 返り値、区間や引数の意味、処理によって変更される引数やメンバ変数などを簡潔に書く。
  - 必要な前提や境界の扱いを書く。
  - ソースコードによる例だけで説明を済ませない。
- `## 計算量` の節で、計算量の種類を「時間」または「空間」と略記する場合は、`時間 $O(n)$` のようにオーダーよりも前に書く（`$O(n)$ 時間` などでない）。
- `## 計算量` の節で big-O 記法などのランダウの記法を用いる場合は、その数学的な定義に従って記載する。
  - 関数 $f$ を用いて計算量オーダーを $O(f)$ と記述する場合、 $f$ は正しい上界でなければならない（悪い例は、入力サイズ $n$ についての漸近計算量オーダーが実際は $\Theta(n\log n)$ であるのにもかかわらず、 $O(n)$ と記述するなどである）。
  - 関数 $f$ を用いて計算量オーダーを $\Theta(f)$ と記述する場合、計算量オーダーは実際に $f$ で上からも下からも抑えられなければならない（悪い例は、入力サイズ $n$ についての漸近計算量オーダーが実際は $O(n)$ であるのにもかかわらず、 $\Theta(n \log n)$ と記述するなどである）。
  - 関数 $f$ を用いて計算量オーダーを $\Omega(f)$ と記述する場合、 $f$ は正しい下界でなければならない。
  - ランダウの記法を用いる場合、非負整数 $N$ に対する $(\log N)^{-1}$ のように値が小さい場合に未定義になる式を用いてもよい。特に、多変数のランダウの記法であっても、一部の変数だけを無限大へ近づけ、他の変数を小さい値に固定した場合まで考慮して過度に複雑な式にする必要はない。
  - big-O 記法を用いて厳密に記述するのが難しい計算量であれば、適宜 $\tilde{O}$ や $\mathrm{poly}$ などを用いること。
  - このライブラリの実装が特定の定数に対して特殊化されており、計算量がその定数に比例する場合は、ランダウの記法以外を用いてその旨を説明する（悪い例は、 $O(128)$ などである）。
- `## 計算量` の節で、最悪計算量または償却計算量ではなく、平均計算量や期待計算量などを述べる場合は、その種類を明記する。
- 制約など、数式で表すべき内容は、バッククォートで囲むのではなく TeX 数式として書く。
  - 1 文字の値（数学定数などローマン体で表記されるべき値を除く）はイタリック体にする（`\mathrm{ }` などを使用しない）。
  - 引数名など、通常の数式では使用しない 2 文字以上の名前を数式中で使用する場合は、先に別の文字で表すことを定義する。
    - その文字には、イタリック体の 1 文字、またはイタリック体の 1 文字に上付きか下付きの数や文字列を付けたものを使用する。
    - 上付きまたは下付きの文字列が英字 1 文字（数学定数などローマン体で表記されるべき値を除く）であれば、イタリック体にする（`\mathrm{ }` などを使用しない）。
    - 上付きまたは下付きの文字列が、ローマン体で表記されるべき値または 2 文字以上の文字列であれば、`\mathrm{ }` を使用する。
- ライブラリのドキュメント中の C++ ソースコードの例は ` ```C++ ` から ` ``` ` までによって囲む（特に、先頭は ` ``` ` や ` ```c++ `、` ```cpp ` などでない）。

## リポジトリのドキュメント

- リポジトリ内のファイルやディレクトリのパスは、先頭に `./` を付けずにリポジトリのルートからの相対パスで記述する。
- リポジトリ内のファイルやディレクトリのパスを、バッククォートで囲まない。
- リポジトリ内のディレクトリのパスは、末尾に `/` を付けて記述する。
- リポジトリ内のファイルやディレクトリのパスは、`[path/to/file](path/to/file)` の形でリンクにする。
- リンクの表示文字列がリポジトリ内のファイルやディレクトリのパスである場合は、リンク先にも全く同じパスを使用する（先頭の `./` の有無や末尾の `/` の有無も一致させる）。
  - `[CC0-1.0 license](LICENSE)` のように、パスでない文字列がリポジトリ内の特定のファイルやディレクトリのパスにリンクされていてもよい。

## 依存関係

- [NotLeonian/competitive-verifier](https://github.com/NotLeonian/competitive-verifier) は、[pyproject.toml](pyproject.toml) の `[tool.uv.sources].competitive-verifier` で、`rev` に 40 文字の commit SHA を指定するか、`tag` にタグ名を指定して固定する（`branch` など、更新される可能性がある参照を指定しない）。
- [.github/workflows/verify.yml](.github/workflows/verify.yml) には、[NotLeonian/competitive-verifier](https://github.com/NotLeonian/competitive-verifier) の commit SHA やタグを直接書かない。

## 設定ファイルなど

- JSON および JSONC は Biome を用いてフォーマットする。
- [.gitignore](.gitignore) では、`# ` から始まるコメントで、その後に並ぶ ignore パターンの内容を説明する。
- [.gitignore](.gitignore) では、`## ` から始まるコメントで、その後に並ぶ記述の由来を説明する。
  - 記述がこのリポジトリ独自のものであれば、他のリポジトリのファイルに由来しない旨を説明する。
  - [github/gitignore](https://github.com/github/gitignore) などの他のリポジトリのファイルに由来する場合は、対象とするプログラミング言語、元となったファイルの permalink の順に記述する。
    - permalink 先の記述に変更を加えている場合は、その変更内容も `## ` から始まるコメントで詳しく説明する。
